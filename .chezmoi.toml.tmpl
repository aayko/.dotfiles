encryption = "age"
[data]
    x11 = true
    wayland = false
    audio = true
    bluetooth = true
    development = true
    desktop = {{ if eq .chezmoi.hostname "archpc" }}true{{ else }}false{{ end }}
    laptop = {{ if eq .chezmoi.hostname "archlaptop" }}true{{ else }}false{{ end }}
    server = {{ if eq .chezmoi.hostname "debian" }}true{{ else }}false{{ end }}
    fonts = true
[age]
    identity = "/home/ayko/.age/key"
    symmetric = true
[git]
    autoCommit = true
    autoPush = true
[diff]
    exclude = ["scripts"]
